#!/bin/sh
# Husky pre-commit hook

echo "🎨 Formatting (Prettier)..."
npx lint-staged

echo "🔍 Linting (ESLint)..."
npx eslint components/ app/ lib/ hooks/ --max-warnings=0

echo "🧪 Running tests (Vitest)..."
npm run test

echo "✅ All checks passed!"
